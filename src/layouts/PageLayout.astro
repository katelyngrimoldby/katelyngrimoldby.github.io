---
import Navigation from '../components/Navigation.astro';
import Slider from '../components/Slider.astro';
---

<header>
  <Navigation />
  <Slider buttonJs='toggle-theme' />
</header>
<main>
  <slot />
</main>
<footer><p>Katelyn Grimoldby © 2025</p></footer>

<style>
  @media screen and (min-width: 0px) {
    header,
    footer {
      padding: 1rem;
      background-color: var(--background-secondary);
      color: var(--foreground-primary);
    }

    header {
      position: sticky;
      top: 0;
      width: 100%;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    main {
      flex-grow: 2;
      padding: 1rem;
      background-color: var(--background-primary);
      color: var(--foreground-primary);
    }

    footer {
      text-align: center;
    }
  }

  @media screen and (min-width: 768px) {
    header,
    footer,
    main {
      padding-inline: 6rem;
    }
  }
</style>

<script>
  const btn = document.querySelector('[data-js="toggle-theme"]');
  const body = document.querySelector('body');

  btn?.addEventListener('click', () => {
    const currTheme = body?.getAttribute('data-theme');
    if (btn)
      btn.className =
        btn.className == 'theme-default' ? 'theme-altered' : 'theme-default';
    body?.setAttribute('data-theme', currTheme == 'dark' ? 'light' : 'dark');
  });
</script>
